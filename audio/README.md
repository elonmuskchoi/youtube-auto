# Audio inputs

Place narration and background music here locally. Audio files are ignored by Git.

- `narration.wav`: final narration with exact timing
- `bgm.wav`: licensed background music
- `word-timings.json`: optional word-level timestamps

