# Landing and Studio captures

Run `scripts/capture_pages.py` after editing `config/pages.example.json`.
Full-page captures are intentionally excluded from Git because they may contain private or copyrighted material.

